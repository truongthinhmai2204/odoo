<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <menuitem id="menu_maintenance_root" name="Maintenance" sequence="10"/>

    
    <record id="view_maintenance_request_form" model="ir.ui.view">
        <field name="name">maintenance.request.custom.form</field>
        <field name="model">maintenance.request.custom</field>
        <field name="arch" type="xml">
            <form string="Maintenance Request Custom">
                <sheet>
                    <group>
                        <field name="name" required="1"/>
                        <field name="equipment_id"/>
                        <field name="maintenance_team_id"/>
                        <field name="request_date"/>
                        <field name="close_date"/>
                        <field name="stage_id" attrs="{'readonly': [('user_id', '!=', uid)]}"/>
                        <field name="category_id"/>
                        <field name="user_id"/>
                        <field name="owner_user_id"/>
                        <field name="product_id"/>
                        <field name="stock_status" readonly="1"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    
    <record id="view_maintenance_request_tree" model="ir.ui.view">
        <field name="name">maintenance.request.custom.tree</field>
        <field name="model">maintenance.request.custom</field>
        <field name="arch" type="xml">
            <tree string="Maintenance Requests Custom">
                <field name="name"/>
                <field name="equipment_id"/>
                <field name="stage_id"/>
                <field name="user_id"/>
                <field name="owner_user_id"/>
                <field name="product_id"/>
                <field name="stock_status"/>
            </tree>
        </field>
    </record>

    
    <record id="action_maintenance_request" model="ir.actions.act_window">
        <field name="name">Maintenance Requests Custom</field>
        <field name="res_model">maintenance.request.custom</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first maintenance request
            </p>
        </field>
    </record>

    <menuitem id="menu_maintenance_request"
                name="Maintenance Requests Custom"
                parent="menu_maintenance_root"
                action="action_maintenance_request"
                sequence="20"/>

</odoo>
